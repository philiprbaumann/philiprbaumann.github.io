Run a thorough design review of the Jekyll site at the current working directory.

Review the following across all collection types (thoughts, movies, books, code, wiki, recipes, playlists, music):

1. **Typography** — font sizes, line heights, heading hierarchy, readability
2. **Spacing** — padding/margin consistency, breathing room between elements
3. **Visual flow** — does the eye move naturally through the page?
4. **Color and contrast** — accessibility, readability
5. **Grid and layout** — alignment, consistent column widths
6. **Cards and list items** — hover states, shadows, borders
7. **Index pages vs detail pages** — are they visually consistent?
8. **Cross-collection consistency** — do all collection types feel like they belong to the same site?

Steps:
1. Read all layout files in `_layouts/` and SCSS files in `_sass/` and `assets/styles/`
2. Identify the top issues with the most visual impact
3. Present findings grouped by severity (high/medium/low)
4. Ask the user which issues to fix before making any changes
5. Apply fixes one area at a time, showing a summary of what changed

$ARGUMENTS
